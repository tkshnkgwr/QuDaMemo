# コントリビューションガイド (CONTRIBUTING.md) - QuDaMemo

## 1. 開発ガイドライン
1. 破壊的変更の禁止（指示された箇所以外のスタイル・挙動を変更しない）。
2. 修正箇所は必ず `// UPDATE [日付]: [内容]` の形式で注釈コメントを付与する。
3. コード変更時は日本語および英語のドキュメント（`docs/ja/`, `docs/en/`）を併せて更新する。

## 2. コミット規約
- `feat:` 新機能
- `fix:` バグ修正
- `docs:` ドキュメント整備
- `refactor:` リファクタリング
